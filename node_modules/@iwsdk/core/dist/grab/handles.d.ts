/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { HandleOptions, HandleStore } from '@pmndrs/handle';
import { Types } from '../ecs/index.js';
import { Object3D, Quaternion, Vector3 } from '../runtime/index.js';
export declare const Handle: import("elics").Component<{
    instance: {
        type: Types.Object;
        default: undefined;
    };
}>;
/** MovementMode for {@link DistanceGrabbable}. @category Grab */
export declare const MovementMode: {
    /** Object smoothly moves with the ray cast end point of the grabbing controller. */
    MoveFromTarget: string;
    /** Object smoothly moves toward the input source that's grabbing. */
    MoveTowardsTarget: string;
    /** Object moves relative to controller delta movement while maintaining distance. */
    MoveAtSource: string;
    /** Object rotates in place without translation or scaling. */
    RotateAtSource: string;
};
export declare class DistanceGrabHandle<T> extends HandleStore<T> {
    readonly target_: Object3D | {
        current?: Object3D | null;
    };
    readonly sceneRoot_: Object3D;
    readonly getOptions: () => HandleOptions<T>;
    readonly movementMode: string;
    readonly returnToOrigin: boolean;
    readonly moveSpeedFactor: number;
    readonly targetPosOffset: Vector3;
    readonly targetQuatOffset: Quaternion;
    readonly detachOnGrab: boolean;
    private previousPointerOrigin;
    private isSnapped;
    private static SNAP_THRESHOLD;
    private static MOVE_SPEED_SCALE;
    private static _tmp;
    private static _tmpQuat;
    private static _posHelper;
    private static _quatHelper;
    private static _offsetPosHelper;
    private static _offsetQuatHelper;
    constructor(target_: Object3D | {
        current?: Object3D | null;
    }, sceneRoot_: Object3D, getOptions: (() => HandleOptions<T>) | undefined, movementMode: string, returnToOrigin: boolean, moveSpeedFactor: number | undefined, targetPosOffset: Vector3 | undefined, targetQuatOffset: Quaternion | undefined, detachOnGrab: boolean);
    update(time: number): void;
    protected apply(target: Object3D): T;
}
//# sourceMappingURL=handles.d.ts.map