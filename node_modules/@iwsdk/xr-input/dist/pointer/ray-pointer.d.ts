/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Pointer } from '@pmndrs/pointer-events';
import { CylinderGeometry, Intersection, Mesh, PerspectiveCamera, ShaderMaterial } from 'three';
import { XROrigin } from '../rig/xr-origin.js';
export declare enum RayDisplayMode {
    Visible = 1,
    VisibleOnIntersection = 2,
    Invisible = 3
}
export declare class RayPointer {
    pointer: Pointer;
    ray: Mesh<CylinderGeometry, ShaderMaterial>;
    enabled: boolean;
    rayIntersection: Intersection | undefined;
    rayDisplayMode: RayDisplayMode;
    constructor(camera: PerspectiveCamera, xrOrigin: XROrigin, handedness: 'left' | 'right');
    update(connected: boolean, delta: number, _time: number, selectStart: boolean, selectEnd: boolean, policy?: {
        forceHideRay?: boolean;
    }): void;
    private updatePointerRendering;
    get busy(): boolean;
    /**
     * Dispose of ray pointer resources.
     */
    dispose(): void;
}
//# sourceMappingURL=ray-pointer.d.ts.map