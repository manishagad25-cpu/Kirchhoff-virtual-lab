import { RagdollBoneProperties } from "@babylonjs/core/Physics/v2/ragdoll";
export interface IRagDollConfiguration {
    assetRelativePath?: string;
    rootNodeId: string;
    skeletonName?: string;
    scalingFactor: number;
    runtimeConfiguration: IRagdollRuntimeConfiguration[];
}
export interface IRagdollRuntimeConfiguration extends RagdollBoneProperties {
    name: string;
    bones: string[];
}
export declare function parseRagdollConfiguration(configuration: IRagDollConfiguration): IRagDollConfiguration;
export declare function copyAndParseRagdollConfiguration(configuration: IRagDollConfiguration): IRagDollConfiguration;
