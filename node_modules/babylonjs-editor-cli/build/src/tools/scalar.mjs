/**
 * Returns the power of two sizes until the given limit.
 * @param limit The limit size.
 * @param from The starting size. Default is 1.
 * @returns An array of power of two sizes.
 */
export function getPowerOfTwoSizesUntil(limit = 4096, from) {
    let size = from ?? 1;
    const result = [];
    while (size <= limit) {
        result.push(size);
        size <<= 1;
    }
    return result;
}
export function getPowerOfTwoUntil(limit) {
    let size = 1;
    while (size <= limit) {
        size <<= 1;
    }
    return size >> 1;
}
//# sourceMappingURL=scalar.mjs.map